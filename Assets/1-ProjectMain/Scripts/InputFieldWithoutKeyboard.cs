


using UnityEngine;
using UnityEngine.UI;
 
public class InputFieldWithoutKeyboard : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
     //  this.GetComponent<InputField>().keyboardType = (TouchScreenKeyboardType)(-4);
     }    
 }



