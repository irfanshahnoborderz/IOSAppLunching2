
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class dummy : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        /*
        
               var txCount = await web3.Eth.Transactions.GetTransactionCount.SendRequestAsync(MyAccountAddress);
            string privateKey = "de7f4af14effb718763e52381184674447d579c0710d857e8dbebfffe90fa02c";
             var encoded = Web3.OfflineTransactionSigner.SignTransaction(privateKey, MyAccountAddress, 10, txCount.Value);
            print(encoded);
             var SendedAdres = Web3.OfflineTransactionSigner.GetSenderAddress(encoded);
              WalletConnectView.instance.SignedText.text = "Signed Msg before  =  " + SendedAdres.ToString();

         
          
        



        */
    }
}
